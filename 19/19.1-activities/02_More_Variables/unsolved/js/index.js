// Create the addNumbers function here.
// function addNumbers() {};
    // between {} add the folowing tasks:
        // var two = 2;
        // var three = 3;
        // var five = two + three;
        // console.log(five); 

    function addNumbers() {
        var two = 2;
        var three = 3;
        var five = two + three;
        console.log(five);
    }

    addNumbers();
    addNumbers();


// After {} this line should be out side of the function
    // addNumbers();


// Step 2: Perform String Concatenation
// Create the stringConcat function here.
// function stringConcat() {};
    // between {} add the folowing tasks:
        // var taco = 'taco';
        // var cat = 'cat';
        // var tacoCat = taco + ' ' + cat;
        // alert(tacoCat);

        function stringContact() {
            var taco = 'taco';
            var cat = 'cat';
            var tacoCat = taco + ' ' + cat;
            alert(tacoCat);
        }




// After {} this line should be out side of the function
    // stringConcat();

        stringContact();